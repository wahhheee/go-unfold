# 第二章：并发编程

## 交付范围

覆盖课程地图的内存模型、Channel 与 sync 原语、Context 与泄漏、背压限流任务池、竞态与并发测试五组主题，拆为十一节。每节包含具体场景、机制、反例、至少三道逐项解释的练习、四组连续追问、交互实验、记忆点、一手资料及真实 Go 验证。

| 节次 | 内容                        | 状态                                                           |
| ---- | --------------------------- | -------------------------------------------------------------- |
| 2.1  | 内存模型与 happens-before   | 已发布：43 项单测、62 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.2  | Channel、缓冲与关闭协议     | 已发布：47 项单测、66 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.3  | select、取消竞争与计时器    | 已发布：50 项单测、70 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.4  | Mutex、RWMutex 与业务不变量 | 已发布：50 项单测、74 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.5  | 原子操作与不可变快照        | 已发布：53 项单测、78 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.6  | WaitGroup、Once 与 Cond     | 已发布：53 项单测、82 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.7  | Context、截止时间与取消原因 | 已发布：57 项单测、86 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.8  | 任务组、流水线与请求合并    | 已发布：57 项单测、90 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.9  | 有界任务池与背压            | 已发布：61 项单测、94 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.10 | 限流、并发额度与等待预算    | 已发布：64 项单测、98 项浏览器检查，Go 测试、race 与 vet 通过  |
| 2.11 | 竞态、泄漏、死锁与验证闭环  | 已发布：68 项单测、102 项浏览器检查，Go 测试、race 与 vet 通过 |

## 质量门槛

- 核验基线 Go 1.27.1 / linux / amd64；依赖扩展库时固定模块版本并记录其契约。
- 每节通过格式、类型、单测、生产构建、Go 测试与竞态检查；新增同步代码执行 vet。
- 每节通过桌面、手机、明暗主题、正文练习与实验流程检查，人工查看截图。
- 浏览器交互是明确限定的教学模型，真实调度、同步保证与故障验证由 `examples/go/` 补充；不得用某次输出证明全部可能执行。
- 每节通过检查后独立提交，使用 Conventional Commits 和简体中文。
- 完成全部十一节后审计规划覆盖、导航、练习归属和维护文档，再提交整章验收记录并停止。

## 整章交付

2026-09-19 完成十一节内容。第二章共 44 道逐项解释的练习、44 组连续追问、11 个交互实验及 11 组真实 Go 验证；加上序章与第一章，应用共 22 篇、84 道题、22 个实验。上表保留每节独立提交时的检查快照。

课程地图的五组规划主题已全部覆盖：2.1 建立同步语义，2.2 至 2.6 展开 Channel 与同步原语，2.7 至 2.8 处理取消和任务责任，2.9 至 2.10 处理容量与准入，2.11 收束诊断与验证。每节明确模型边界，Go 样本提供对应执行证据。

版本基线为 Go 1.27.1 / linux / amd64，扩展库固定 x/sync v0.23.0、x/time v0.16.0。重点纠正无竞态等于业务正确、select 取消优先、Cond 必有虚假唤醒、cancel 等于任务结束、限流等于限并发等误说；保留旧计时器开关移除和泄漏画像的版本限制。

整章入口、搜索规划状态、首尾翻页、练习归属与笔记隔离均纳入浏览器回归。当次验收时第三章保留为规划；2026-09-20 的后续交付见 `NETWORK_CHAPTER.md`。

## 最终验收

- `npm run format:check`、类型检查、68 项单测与生产构建通过。
- 104 项桌面和手机浏览器检查已验证通过，含每节明暗主题、axe、代码高亮、练习、追问和学习记录。最终全量运行先通过 102 项；新增导航检查的 2 项因选择器同时匹配章入口与第一节而失败，限定课节链接后两项复验通过，应用代码无需修正。
- 全部 21 个 Go 包的普通测试、vet、race 与随机测试顺序检查通过；5 秒 fuzz 执行 171,216 次输入，未发现失败，仅代表本次覆盖范围。
- 生产构建另验证第二章 11 个直接路由、按需加载、代码高亮和刷新，并操作交错实验；未出现浏览器异常或资源请求失败。
- 各节桌面与手机截图已人工检查。受限教学模型与真实工具链的边界分别说明；mutex、block、trace 和泄漏画像证据见 `examples/go/diagnostics/RESULTS.md`。
- 十一节均已按 Conventional Commits 和简体中文独立提交；整章完成后同步发布状态、编写指南、架构说明、示例索引与内容更新规范。
