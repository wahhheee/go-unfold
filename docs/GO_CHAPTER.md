# 第一章：Go 语言与运行时

## 交付范围

本章覆盖原课程地图中的全部五组主题，拆成十节。每节包含场景、机制、边界、至少三道逐项解释的练习、连续追问、记忆锚点、官方依据与可复验例子。交互模型明确区分模拟与真实 Go 执行。

| 节次 | 内容                     | 状态                                                               |
| ---- | ------------------------ | ------------------------------------------------------------------ |
| 1.1  | 值、类型与内存布局       | 已发布：Go 示例、28 项单测与 20 项浏览器检查通过                   |
| 1.2  | 接口、方法集与 nil       | 已发布：Go 示例、28 项单测与 24 项浏览器检查通过                   |
| 1.3  | 泛型、类型集与约束       | 已发布：Go 示例、28 项单测与 28 项浏览器检查通过                   |
| 1.4  | 数组、切片与字符串       | 已发布：Go 示例、31 项单测与 32 项浏览器检查通过                   |
| 1.5  | map 与哈希表边界         | 已发布：Go 示例与竞态检查、34 项单测、36 项浏览器检查通过          |
| 1.6  | GMP、调度与抢占          | 已发布：Go 示例与竞态检查、34 项单测、40 项浏览器检查通过          |
| 1.7  | 分配、逃逸与 GC          | 已发布：Go 示例与逃逸诊断、36 项单测、44 项浏览器检查通过          |
| 1.8  | 基准测试与性能剖析       | 已发布：Go 基准与画像验证、36 项单测、48 项浏览器检查通过          |
| 1.9  | 错误、defer 与 panic     | 已发布：Go 错误与子进程验证、36 项单测、52 项浏览器检查通过        |
| 1.10 | 测试、模糊测试与工程闭环 | 已发布：Go 测试、vet、race 与 fuzz，39 项单测、56 项浏览器检查通过 |

## 质量门槛

- 语言保证引用规范；实现细节注明 Go 版本及必要的平台前提。
- 当前核验基线为 Go 1.27，实际执行环境为 Go 1.27.1 / linux / amd64。
- Go 示例在 `examples/go/` 运行验证，前端实验模型独立测试。
- 每节发布前通过格式、类型、模型、Go 示例、构建和浏览器检查，并查看桌面与手机截图。
- 每完成一节单独提交，提交采用 Conventional Commits + 简体中文。
- 本章完成后做覆盖审计，更新序章与项目说明中的发布状态，再停止工作并汇报。

## 整章验收

2026-09-19 完成十节发布。上表的检查数量保留各节提交时的快照；整章最终检查为 41 项单测与 58 项浏览器测试，均通过。第一章有 37 道逐项解释的练习、40 组连续追问和 10 个交互实验；加上序章，应用共发布 11 篇、40 道题。

五组规划均有对应内容：类型、接口与泛型为 1.1 至 1.3；容器与布局为 1.1、1.4、1.5；调度为 1.6；内存与性能为 1.7、1.8；错误和工程测试为 1.9、1.10。各节包含版本前提、反例、记忆点和一手参考资料，实验说明模型边界。

- 前端：格式、类型、模型和注册一致性检查、生产构建通过。
- 浏览器：桌面与手机逐节阅读、所有练习及追问、十个实验、明暗主题、axe、搜索、翻页、笔记隔离与刷新持久化通过；每节及整章导航截图已人工查看。
- Go：十个包的普通测试、vet、race 与随机测试顺序检查通过；往返性质 fuzz 运行 5 秒，执行约 19 万次输入，未发现失败。这只代表本次覆盖范围。
- 诊断：逃逸分析、重复基准、CPU 与内存画像、执行 trace 已实跑，性能结果与限制见 [实跑记录](../examples/go/performance/RESULTS.md)。
- 发布：十节分别提交，序章入口、知识地图、搜索与维护文档已同步；其他模块继续保留规划状态。

实际 Go 工具链为 `go1.27.1-X:nodwarf5`，平台为 linux/amd64。在线一手资料与本地规范、API 记录和源码交叉核对；部分官方页面访问遇到超时或 TLS 失败时，对照本地对应版本资料，不据此宣称全部链接始终可用。GitHub Actions 已配置质量门槛，本次验收来自本地运行，尚未推送远端运行 CI。
